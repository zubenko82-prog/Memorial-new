import React from "react";

export default function App() {
  return (
    <div style={{ padding: 16, color: "#fff", background: "#111", minHeight: "100vh" }}>
      App ok (no Start import)
    </div>
  );
}
